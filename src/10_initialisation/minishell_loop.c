#include "minishell.h"

void    ft_minishell_loop(t_data *env)
{
    while(!env->exit_asked)
    {
        // affichage prompt
        // lecture entrees sur stdin avec readline
        // precheck de la string sur les cas d'erreur 'basiques' (> seul, ppe au debut...)
        // parsing de l'entree (alimentation structure)
        //      -
        // interpretation de l'input et execution des cmd
    }
}